import React from 'react';

const Header: React.FC = () => {
  return (
    <header className="header">
      <h1>AWS CloudFront React App</h1>
      <p>A simple React TypeScript application ready for CloudFront deployment</p>
    </header>
  );
};

export default Header;
